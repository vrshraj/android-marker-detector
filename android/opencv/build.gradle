apply plugin: 'com.android.library'

android {
    namespace 'org.opencv'
    compileSdkVersion 34

    defaultConfig {
        minSdkVersion 24
    }

    publishing {
        multipleVariants {
            allVariants()
        }
    }
}
